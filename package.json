{
  "name": "gst-invoice-pro-root",
  "version": "1.0.0",
  "description": "Root package for Unified Deployment",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
